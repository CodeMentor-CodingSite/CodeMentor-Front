export default function Problem({
  params: { slug },
}: {
  params: { slug: string };
}) {
  return (
    <div className="flex h-screen mt-3">
      {/* <ProblemDescription id={slug} /> */}
    </div>
  );
}
